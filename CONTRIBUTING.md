# Contributing to book-md-tools

- Use Python 3.9+
- Keep fixers deterministic and idempotent
- Prefer config-driven overrides in `configs/`
- Add unit tests where possible (TBD)
- Update README when adding new CLI or tasks
